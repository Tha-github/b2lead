import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ message: "Dispatch automático não disponível nesta versão" });
}

export async function POST() {
  return NextResponse.json({ message: "Dispatch automático não disponível nesta versão" });
}
